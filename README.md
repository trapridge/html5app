html5app
========

To start developing:

 `npm install -g grunt-cli bower`

 `npm install && bower install`

 `grunt server`

TODO:

 * reordering
  * drag&drop
 * categorization
 * hook up with a categorization api
 * lists management
 * visuals 